## Global Docker image parameters
## Please, note that this will override the image parameters, including dependencies, configured to use the global value
## Current available global Docker image parameters: imageRegistry and imagePullSecrets
##
global:
  # -- Global Docker image registry
  imageRegistry:
  # -- Global Docker registry secret names as an array
  ## E.g.
  ## imagePullSecrets:
  ##   - myRegistryKeySecretName
  ##
  imagePullSecrets: []
  # -- Global StorageClass for Persistent Volume(s)
  storageClass:

# -- String to partially override common.names.fullname template (will maintain the release name)
##
nameOverride:

# -- String to fully override common.names.fullname template
##
fullnameOverride:

# -- Force target Kubernetes version (using Helm capabilites if not set)
##
kubeVersion:

## Init databases image version
## ref: https://github.com/open-hand/choerodon-starters/tree/master/choerodon-tool-liquibase
##
initDatabases:
  enabled: true
  timeout: 1800
  # -- DB tool image registry
  registry: registry.cn-shanghai.aliyuncs.com
  # -- DB tool image repository
  repository: c7n/dbtool
  # -- DB tool image tag. Default Chart.AppVersion
  tag: 0.7.5
  # -- Specify a imagePullPolicy.
  # Defaults to 'Always' if image tag is 'latest', else set to 'IfNotPresent'
  ## ref: http://kubernetes.io/docs/user-guide/images/#pre-pulling-images
  ##
  pullPolicy: IfNotPresent
  # -- Optionally specify an array of imagePullSecrets.
  # Secrets must be manually created in the namespace.
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
  ## e.g:
  ## pullSecrets:
  ##   - myRegistryKeySecretName
  ##
  pullSecrets: []
  # -- Excluding update certain tables or fields: table1,table2.column1
  exclusion: ''
  datasource:
    driver: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/?useUnicode=true&characterEncoding=utf-8&useSSL=false&useInformationSchema=true&remarks=true&serverTimezone=Asia/Shanghai
    username: username
    password: password

  datasources:
    platform:
      driver: com.mysql.jdbc.Driver
      url: jdbc:mysql://localhost:3306/?useUnicode=true&characterEncoding=utf-8&useSSL=false&useInformationSchema=true&remarks=true&serverTimezone=Asia/Shanghai
      username: username
      password: password

  # -- Affinity for pod assignment. Evaluated as a template.
  # Note: podAffinityPreset, podAntiAffinityPreset, and nodeAffinityPreset will be ignored when it's set
  ## ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#affinity-and-anti-affinity
  ##
  affinity: {}

  # -- Node labels for pod assignment. Evaluated as a template.
  ## ref: https://kubernetes.io/docs/user-guide/node-selection/
  ##
  nodeSelector: {}

  # -- Tolerations for pod assignment. Evaluated as a template.
  ## ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
  ##
  tolerations: []

## DevOps service image version
## ref: https://github.com/open-hand/devops-service-business
##
image:
  # -- DevOps service image registry
  registry: registry.cn-shanghai.aliyuncs.com
  # -- DevOps service image repository
  repository: c7n/devops-service-business
  # -- (string) DevOps service image tag. Default Chart.AppVersion
  tag: ~
  # -- Specify a imagePullPolicy.
  # Defaults to 'Always' if image tag is 'latest', else set to 'IfNotPresent'
  ## ref: http://kubernetes.io/docs/user-guide/images/#pre-pulling-images
  ##
  pullPolicy: IfNotPresent
  # -- Optionally specify an array of imagePullSecrets.
  # Secrets must be manually created in the namespace.
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
  ## e.g:
  ## pullSecrets:
  ##   - myRegistryKeySecretName
  ##
  pullSecrets: []

## Service operating environment
##
base:
  # -- Java base image registry
  registry: registry.cn-shanghai.aliyuncs.com
  # -- Java base image repository
  repository: c7n/javabase
  # -- Java base image tag
  tag: jdk8u282-b08
  # -- Specify a imagePullPolicy
  ## Defaults to 'Always' if image tag is 'latest', else set to 'IfNotPresent'
  ## ref: http://kubernetes.io/docs/user-guide/images/#pre-pulling-images
  ##
  pullPolicy: IfNotPresent
  # -- Optionally specify an array of imagePullSecrets.
  ## Secrets must be manually created in the namespace.
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
  ## e.g:
  ## pullSecrets:
  ##   - myRegistryKeySecretName
  ##
  pullSecrets: []

# -- Add labels to all the deployed resources
##
commonLabels: {}

# -- Add annotations to all the deployed resources
##
commonAnnotations: {}

## An array to add extra env vars
## e.g:
## extraEnv:
##   FOO: "bar"
##
extraEnv: 
  # -- Redis host
  SPRING_REDIS_HOST: localhost
  # -- Redis port
  SPRING_REDIS_PORT: 6379
  # -- Redis database
  SPRING_REDIS_DATABASE: 9
  # -- Eureka client service url
  EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://register-server:8000/eureka/
  # -- Datasource url
  SPRING_DATASOURCE_URL: jdbc:mysql://localhost:3306/devops_service?useUnicode=true&characterEncoding=utf-8&useSSL=false&useInformationSchema=true&remarks=true&serverTimezone=Asia/Shanghai
  # -- Datasource username
  SPRING_DATASOURCE_USERNAME: choerodon
  # -- Datasource password
  SPRING_DATASOURCE_PASSWORD: password
  # -- Gitlab http(s) url
  SERVICES_GITLAB_URL: http://git.example.com
  # -- Gitlab ssh url
  SERVICES_GITLAB_SSHURL: git@git.example.com
  # -- Gitlab password reset url
  SERVICES_GITLAB_RESETPASSWORDURL: git.example.com/resetpassword
  # -- Helm chart url
  SERVICES_HELM_URL: http://helm.example.com
  # -- Helm chart username
  SERVICES_HELM_USERNAME:
  # -- Helm chart password
  SERVICES_HELM_PASSWORD:
  # -- Choerodon api url
  SERVICES_GATEWAY_URL: http://api.example.com
  # -- Choerodon front url
  SERVICES_FRONT_URL: http://app.example.com
  # -- choerodon-cluster-agent version
  AGENT_VERSION: 2.0.0
  # -- devops-service-business websocket url
  AGENT_SERVICEURL: ws://devops.example.com/websocket
  # -- agent helm chart url
  AGENT_REPOURL: https://openchart.choerodon.com.cn/choerodon/c7n/
  # -- cert-manager helm chart url
  AGENT_CERTMANAGERURL: https://openchart.choerodon.com.cn/choerodon/c7n/
  # -- whether to enable the configuration center
  SPRING_CLOUD_CONFIG_ENABLED: false
  # -- configuration center url
  SPRING_CLOUD_CONFIG_URI: http://register-server:8000
  # -- host agent url
  C7N_AGENT_BINARY_URL: http://example.com/c7n-agent-2.0.0
  # -- host agent version
  C7N_AGENT_VERSION: 2.0.0
  # -- feign client config default connect timeout
  FEIGN_CLIENT_CONFIG_DEFAULT_CONNECT_TIMEOUT: 2000
  # -- feign client config default read timeout
  FEIGN_CLIENT_CONFIG_DEFAULT_READ_TIMEOUT: 5000
  # -- Gitlab ssh internal url
  SERVICES_GITLAB_INTERNALSSHURL:
  # -- Gitlab users can create the number of project
  SERVICES_GITLAB_PROJECTLIMIT: 0
  # -- Agent repo concurrency syncsize
  AGENT_REPOCONCURRENCYSYNCSIZE: 1
  # 百分比使用物理内存，预留一部分用于堆外内存以及外部程序
  JAVA_OPTS: -XX:MaxRAMPercentage=90.0
  # -- Ignore the interface path of verification
  CHOERODON_RESOURCE_JWT_IGNORE: >-
    /workflow/**, /sonar/**, /ci, /sonar/info, /v2/api-docs,
    /agent/**, /ws/**, /gitlab/email, /webhook/**, /v2/choerodon/**,
    /choerodon/**, /actuator/**, /prometheus, /devops/**, /pre_stop
  CI_DFAULT_RULE_NUMBER: 5
  # ci流水线中构建镜像的方式，可选值:kaniko、docker
  CI_IMAGE_BUILD_TYPE: kaniko
  LOG_LEVEL: info
  NEXUS_PROXY_URIPREFIX: /rdupm/v1/nexus/proxy
  DEVOPS_CI_MAVEN_PROXY_ID: default
  DEVOPS_CI_MAVEN_PROXY_ACTIVE: false
  DEVOPS_CI_MAVEN_PROXY_PROTOCOL: http
  DEVOPS_CI_MAVEN_PROXY_USERNAME: proxyuser
  DEVOPS_CI_MAVEN_PROXY_PASSWORD: proxypass
  DEVOPS_CI_MAVEN_PROXY_HOST: proxy.host.net
  DEVOPS_CI_MAVEN_PROXY_PORT: 80
  # 流水线执行代码扫描的用户token.需要Analysis和Create权限
  SERVICES_SONARQUBE_ANALYSIS_USER_TOKEN:
  # api测试的runner镜像
  TEST_RUNNER_IMAGE:
  # 主机部署超时时间，单位是分钟
  CI_HOST_DEPLOY_TIMEOUT: 10

# -- ConfigMap with extra environment variables
##
extraEnvVarsCM: ""

# -- Secret with extra environment variables
##
extraEnvVarsSecret: ""

# -- Container's working directory(Default mountPath).
##
workingDir: /opt/choerodon

# -- Command for running the devops-service-business container (set to default if not set). Use array form
##
command: []

# -- Args for running the devops-service-business container (set to default if not set). Use array form
##
args: []

# -- Scheduler name
## https://kubernetes.io/docs/tasks/administer-cluster/configure-multiple-schedulers/
##
schedulerName: 

# -- EnableServiceLinks indicates whether information about services should be injected into pod's environment variables, 
# matching the syntax of Docker links. Optional: Defaults to false.
## 
enableServiceLinks: false

# -- AutomountServiceAccountToken indicates whether a service account token should be automatically mounted.
## 
automountServiceAccountToken: false

## devops-service-business deployment parameters
## The deployment strategy to use to replace existing pods with new ones.
## ref: https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#strategy
## e.g:
## updateStrategy:
##  type: Recreate
##
updateStrategy:
  # -- Type of deployment. Can be "Recreate" or "RollingUpdate". Default is RollingUpdate.
  type: RollingUpdate
  # -- Rolling update config params. Present only if DeploymentStrategyType = RollingUpdate.
  rollingUpdate:
    maxSurge: 100%
    maxUnavailable: 0

# -- Number of deployment replicas
##
replicaCount: 1

# -- devops-service-business pod host aliases
## https://kubernetes.io/docs/concepts/services-networking/add-entries-to-pod-etc-hosts-with-host-aliases/
##
hostAliases: []

## devops-service-business container ports to open
##
containerPort: 
  # -- DevOps server port
  serverPort: 8060
  # -- DevOps server management port
  actuatorPort: 8061

# -- Security Context
## ref: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/
##
securityContext:
  enabled: true
  fsGroup: 33
  runAsUser: 33

# -- Pod labels
## Ref: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
##
podLabels: {}

# -- Pod annotations
## ref: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
##
podAnnotations: {}

# -- Pod affinity preset.
# Allowed values: soft, hard
## ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#inter-pod-affinity-and-anti-affinity
##
podAffinityPreset: ""

# -- Pod anti-affinity preset.
# Allowed values: soft, hard
## ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#inter-pod-affinity-and-anti-affinity
##
podAntiAffinityPreset: soft

## Node affinity preset.
## Allowed values: soft, hard
## ref: https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity
##
nodeAffinityPreset:
  # -- Node affinity type.
  # Allowed values: soft, hard
  ##
  type: ""
  # -- Node label key to match
  ## E.g.
  ## key: "kubernetes.io/e2e-az-name"
  ##
  key: ""
  # -- Node label values to match
  ## E.g.
  ## values:
  ##   - e2e-az1
  ##   - e2e-az2
  ##
  values: []

# -- Affinity for pod assignment. Evaluated as a template.
# Note: podAffinityPreset, podAntiAffinityPreset, and nodeAffinityPreset will be ignored when it's set
## ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#affinity-and-anti-affinity
##
affinity: {}

# -- Node labels for pod assignment. Evaluated as a template.
## ref: https://kubernetes.io/docs/user-guide/node-selection/
##
nodeSelector: {}

# -- Tolerations for pod assignment. Evaluated as a template.
## ref: https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/
##
tolerations: []

## devops-service-business containers' resource requests and limits
## ref: http://kubernetes.io/docs/user-guide/compute-resources/
##
resources:
  ## We usually recommend not to specify default resources and to leave this as a conscious
  ## choice for the user. This also increases chances charts run on environments with little
  ## resources, such as Minikube. If you do want to specify resources, uncomment the following
  ## lines, adjust them as necessary, and remove the curly braces after 'resources:'.

  # -- The resources limits for the init container
  limits: 
    # cpu: 2
    memory: 3Gi

  # -- The requested resources for the init container
  requests: 
    # cpu: 500m
    memory: 3Gi

## Configure extra options for liveness, readiness and startup probes
## ref: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/#configure-probes
##
livenessProbe:
  # -- Enable livenessProbe
  enabled: true
  # -- Initial delay seconds for livenessProbe
  initialDelaySeconds: 300
  # -- Period seconds for livenessProbe
  periodSeconds: 5
  # -- Timeout seconds for livenessProbe
  timeoutSeconds: 3
  # -- Success threshold for livenessProbe
  successThreshold: 1
  # -- Failure threshold for livenessProbe
  failureThreshold: 2
readinessProbe:
  # -- Enable readinessProbe
  enabled: true
  # -- Initial delay seconds for readinessProbe
  initialDelaySeconds: 20
  # -- Period seconds for readinessProbe
  periodSeconds: 5
  # -- Timeout seconds for readinessProbe
  timeoutSeconds: 3
  # -- Success threshold for readinessProbe
  successThreshold: 1
  # -- Failure threshold for readinessProbe
  failureThreshold: 2
startupProbe:
  # -- Enable startupProbe
  enabled: true
  # -- Initial delay seconds for startupProbe
  initialDelaySeconds: 10
  # -- Period seconds for startupProbe
  periodSeconds: 3
  # -- Timeout seconds for startupProbe
  timeoutSeconds: 2
  # -- Success threshold for startupProbe
  successThreshold: 1
  # -- Failure threshold for startupProbe
  failureThreshold: 60

## Custom Liveness, Readiness, and Startup probes for devops-service-business
##
# customLivenessProbe -- Custom Liveness
customLivenessProbe: {}
# -- Custom Readiness
customReadinessProbe: {}
# -- Custom Startup probes
customStartupProbe: {}

# -- Extra volumes to add to the devops-service-business statefulset
##
extraVolumes: []

# -- Extra volume mounts to add to devops-service-business containers
##
extraVolumeMounts: []

# -- Add init containers to the devops-service-business pods.
## e.g:
## initContainers:
##   - name: your-image-name
##     image: your-image
##     imagePullPolicy: Always
##     ports:
##       - name: portname
##         containerPort: 1234
##
initContainers: {}

## Skywalking options
skywalking:
  # -- Enable skywalking
  enabled: false
  # -- Skywalking image registry
  registry: registry.cn-shanghai.aliyuncs.com
  # -- Skywalking image repository
  repository: c7n/skywalking-agent
  # -- Skywalking image tag
  tag: 8.10.0
  # -- Specify a imagePullPolicy
  # Defaults to 'Always' if image tag is 'latest', else set to 'IfNotPresent'
  ## ref: http://kubernetes.io/docs/user-guide/images/#pre-pulling-images
  ##
  pullPolicy: IfNotPresent
  # -- Optionally specify an array of imagePullSecrets.
  # Secrets must be manually created in the namespace.
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
  ## e.g:
  ## pullSecrets:
  ##   - myRegistryKeySecretName
  ##
  pullSecrets: []

  ## Skywalking Agent Configuration
  ## ref: https://github.com/apache/skywalking/blob/master/docs/en/setup/service-agent/java-agent/README.md#table-of-agent-configuration-properties

  # skywalking.serviceName -- The serviceName (Default .Chart.Name) to represent a logic group providing the same capabilities/logic. 
  # Suggestion: set a unique name for every logic service group, service instance nodes share the same code,Max length is 50(UTF-8 char).
  serviceName: 

  # -- Negative or zero means off, by default. sampleNPer3Secs means sampling N TraceSegment in 3 seconds tops.
  sampleNPer3Secs: 9

  # -- Collector SkyWalking trace receiver service addresses.
  collectorService: oap.skywalking:11800

  # -- String to fully override Skywalking Agent Configuration template
  commandOverride: 

# -- Add sidecars to the devops-service-business pods.
## e.g:
## sidecars:
##   - name: your-image-name
##     image: your-image
##     imagePullPolicy: Always
##     ports:
##       - name: portname
##         containerPort: 1234
##
sidecars: {}

## devops-service-business Service properties
##
service:
  # -- Set to true to enable service record generation
  ##
  enabled: true
  # -- devops-service-business Service type
  ##
  type: ClusterIP
  # -- devops-service-business Service port
  ##
  port: 
    server: 8060
    actuator: 8061
  # -- Specify the nodePort value for the LoadBalancer and NodePort service types.
  ## ref: https://kubernetes.io/docs/concepts/services-networking/service/#type-nodeport
  ##
  nodePort:
    server: 30138
    actuator: 30139
  # -- loadBalancerIP for the devops-service-business Service (optional, cloud specific)
  ## ref: http://kubernetes.io/docs/user-guide/services/#type-loadbalancer
  ##
  loadBalancerIP:
  # -- Load Balancer sources
  ## https://kubernetes.io/docs/tasks/access-application-cluster/configure-cloud-provider-firewall/#restrict-access-for-loadbalancer-service
  ## e.g:
  ## loadBalancerSourceRanges:
  ##   - 10.10.10.0/24
  ##
  loadBalancerSourceRanges: []
  # -- Enable client source IP preservation
  ## ref http://kubernetes.io/docs/tasks/access-application-cluster/create-external-load-balancer/#preserving-the-client-source-ip
  ##
  externalTrafficPolicy: Cluster
  # -- Provide any additional annotations which may be required. This can be used to
  # set the LoadBalancer service type to internal only.
  ## ref: https://kubernetes.io/docs/concepts/services-networking/service/#internal-load-balancer
  ##
  annotations: {}

## Discourse ingress parameters
## ref: https://kubernetes.io/docs/user-guide/ingress/
##
ingress:
  # -- Enable ingress record generation for Discourse
  ##
  enabled: false
  # -- IngressClass that will be be used to implement the Ingress (Kubernetes 1.18+)
  ## This is supported in Kubernetes 1.18+ and required if you have more than one IngressClass marked as the default for your cluster .
  ## ref: https://kubernetes.io/blog/2020/04/02/improvements-to-the-ingress-api-in-kubernetes-1.18/
  ##
  ingressClassName: ""
  # -- Ingress path type
  ##
  pathType: ImplementationSpecific
  # -- Force Ingress API version (automatically detected if not set)
  ##
  apiVersion: ""
  # -- Default host for the ingress record
  ##
  hostname: server.local
  # -- Default path for the ingress record
  ## NOTE: You may need to set this to '/*' in order to use this with ALB ingress controllers
  ##
  path: /
  # -- Additional annotations for the Ingress resource. To enable certificate autogeneration, place here your cert-manager annotations.
  ## Use this parameter to set the required annotations for cert-manager, see
  ## ref: https://cert-manager.io/docs/usage/ingress/#supported-annotations
  ## e.g:
  ## annotations:
  ##   kubernetes.io/ingress.class: nginx
  ##   cert-manager.io/cluster-issuer: cluster-issuer-name
  ##
  annotations: {}
  # -- Enable TLS configuration for the host defined at `ingress.hostname` parameter
  ## TLS certificates will be retrieved from a TLS secret with name: `{{- printf "%s-tls" .Values.ingress.hostname }}`
  ## You can:
  ##   - Use the `ingress.secrets` parameter to create this TLS secret
  ##   - Relay on cert-manager to create it by setting the corresponding annotations
  ##   - Relay on Helm to create self-signed certificates by setting `ingress.selfSigned=true`
  ##
  tls: false
  # -- Create a TLS secret for this ingress record using self-signed certificates generated by Helm
  ##
  selfSigned: false
  # -- An array with additional hostname(s) to be covered with the ingress record
  ## e.g:
  ## extraHosts:
  ##   - name: discourse.local
  ##     path: /
  ##
  extraHosts: []
  # -- An array with additional arbitrary paths that may need to be added to the ingress under the main host
  ## e.g:
  ## extraPaths:
  ## - path: /*
  ##   backend:
  ##     serviceName: ssl-redirect
  ##     servicePort: use-annotation
  ##
  extraPaths: []
  # -- TLS configuration for additional hostname(s) to be covered with this ingress record
  ## ref: https://kubernetes.io/docs/concepts/services-networking/ingress/#tls
  ## e.g:
  ## extraTls:
  ## - hosts:
  ##     - discourse.local
  ##   secretName: discourse.local-tls
  ##
  extraTls: []
  # -- Custom TLS certificates as secrets
  ## NOTE: 'key' and 'certificate' are expected in PEM format
  ## NOTE: 'name' should line up with a 'secretName' set further up
  ## If it is not set and you're using cert-manager, this is unneeded, as it will create a secret for you with valid certificates
  ## If it is not set and you're NOT using cert-manager either, self-signed certificates will be created valid for 365 days
  ## It is also possible to create and manage the certificates outside of this helm chart
  ## Please see README.md for more information
  ## e.g:
  ## secrets:
  ##   - name: discourse.local-tls
  ##     key: |-
  ##       -----BEGIN RSA PRIVATE KEY-----
  ##       ...
  ##       -----END RSA PRIVATE KEY-----
  ##     certificate: |-
  ##       -----BEGIN CERTIFICATE-----
  ##       ...
  ##       -----END CERTIFICATE-----
  ##
  secrets: []

## Enable persistence using Persistent Volume Claims
## ref: http://kubernetes.io/docs/user-guide/persistent-volumes/
##
persistence:
  # -- If true, use a Persistent Volume Claim, If false, use emptyDir
  ##
  enabled: false
  # -- Persistent Volume Storage Class
  ## If defined, storageClassName: <storageClass>
  ## If set to "-", storageClassName: "", which disables dynamic provisioning
  ## If undefined (the default) or set to null, no storageClassName spec is
  ##   set, choosing the default provisioner.  (gp2 on AWS, standard on
  ##   GKE, AWS & OpenStack)
  ##
  storageClass: 

  # -- Data volume mount path
  ##
  mountPath: /data
  # -- Persistent Volume Access Mode
  ##
  accessModes:
    - ReadWriteOnce
  # -- Persistent Volume size
  ##
  size: 8Gi
  # -- Persistent Volume Claim annotations
  ##
  annotations: {}
  # -- Enable persistence using an existing PVC
  ##
  existingClaim: 

# -- Change the owner and group of the persistent volume mountpoint to runAsUser:fsGroup values from the securityContext section.
volumePermissionsEnabled: false

## Specifies whether a ServiceAccount should be created
##
serviceAccount:
  # -- Set to true to create serviceAccount
  ##
  create: false
  # -- The name of the ServiceAccount to use.
  ## If not set and create is true, a name is generated using the common.names.fullname template
  ##
  name: ""
