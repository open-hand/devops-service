<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.choerodon.devops.infra.mapper.DevopsCdApiTestInfoMapper">
  <select id="doesApiTestSuiteRelatedWithPipeline" resultType="java.lang.Boolean">
    SELECT COUNT(*) > 0
    FROM devops_cd_job cdj
                 JOIN devops_cd_api_test_info dcati ON dcati.id = cdj.deploy_info_id
    WHERE dcati.api_test_suite_id = #{suiteId}
  </select>
</mapper>

