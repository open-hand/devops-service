<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.choerodon.devops.infra.mapper.AppServiceHelmVersionMapper">
    <insert id="batchInsert">
        insert ignore into devops_app_service_helm_version(app_service_version_id,
        helm_config_id,
        harbor_repo_type,
        harbor_config_id,
        value_id,
        readme_value_id,
        image,
        repository)
        values
        <foreach collection="appServiceHelmVersionDTOToInsert" item="version" separator="," index="i">
            (#{version.appServiceVersionId},
            #{version.helmConfigId},
            #{version.harborRepoType},
            #{version.harborConfigId},
            #{version.valueId},
            #{version.readmeValueId},
            #{version.image},
            #{version.repository})
        </foreach>
    </insert>

    <select id="listByAppVersionIds" resultType="io.choerodon.devops.api.vo.appversion.AppServiceHelmVersionVO">
        select *
        from devops_app_service_helm_version dashv
        where dashv.app_service_version_id in
        <foreach collection="versionIds" index="i" open="(" separator="," close=")" item="versionId">
            #{versionId}
        </foreach>
    </select>
</mapper>

