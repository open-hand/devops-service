<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.choerodon.devops.infra.mapper.DevopsCiJobRecordMapper">


    <select id="batchListByCiPipelineRecordId" resultType="io.choerodon.devops.api.vo.DevopsCiJobRecordVO">
        SELECT *
        FROM devops_ci_job_record dcjr
        WHERE dcjr.ci_pipeline_record_id IN
        <foreach collection="pids" open="(" close=")" separator="," item="pid">
            pid
        </foreach>
    </select>
</mapper>