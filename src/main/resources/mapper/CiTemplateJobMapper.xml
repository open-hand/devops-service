<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.choerodon.devops.infra.mapper.CiTemplateJobMapper">

    <select id="listByStageId" resultType="io.choerodon.devops.infra.dto.CiTemplateJobDTO">
        select dctj.*
        from devops_ci_template_stage_job_rel dctsjr
                 join devops_ci_template_job dctj on dctj.id = dctsjr.ci_template_job_id
        where dctsjr.ci_template_stage_id = #{stageId}
          AND dctj.visibility=1
    </select>
    <select id="listByStageIds" resultType="io.choerodon.devops.api.vo.template.CiTemplateJobVO">
        select dctj.id,
        dctj.name,
        dctj.group_id,
        dctj.image,
        dctj.source_type,
        dctj.source_id,
        dctj.built_in,
        dctj.visibility,
        dctj.type,
        dctj.trigger_value,
        dctj.trigger_type,
        dctj.script,
        dctj.to_upload,
        dctj.to_download,
        dctj.parallel,
        dctj.is_enabled as enabled,
        dctj.config_id,
        dctj.tags,
        dctj.config_file_id,
        dctj.config_file_path,
        dctj.start_in,
        dctj.object_version_number,
        dctj.created_by,
        dctj.creation_date,
        dctj.last_updated_by,
        dctj.last_update_date,
        dctsjr.ci_template_stage_id as relate_stage_id , dctsjr.sequence
        from devops_ci_template_stage_job_rel dctsjr
        join devops_ci_template_job dctj on dctj.id = dctsjr.ci_template_job_id
        where dctsjr.ci_template_stage_id in
        <foreach collection="stageIds" item="stageId" open="(" separator="," close=")">
            #{stageId}
        </foreach>
--         AND dctj.visibility=1
    </select>
    <select id="listByTenantIdAndGroupId" resultType="io.choerodon.devops.infra.dto.CiTemplateJobDTO">
        select *
        from devops_ci_template_job dctj
        where dctj.group_id = #{groupId}
          and ((dctj.source_type = 'site' and dctj.source_id = 0)
            or (dctj.source_type = 'organization' and dctj.source_id = #{organizationId})
            or (dctj.source_type = 'project' and dctj.source_id = #{projectId})
            )
          AND dctj.visibility = 1
    </select>
    <select id="listConfigFileReferenceInfo" resultType="io.choerodon.devops.api.vo.template.CiTemplateJobVO">
        select dctj.*
        from devops_ci_template_job dctj
                 join devops_ci_tpl_job_config_file_rel dctjcfr
                      on dctjcfr.ci_template_job_id = dctj.id and dctjcfr.config_file_id = #{configFileId}
    </select>

    <update id="updateImageByIds">
        UPDATE devops_ci_template_job
        SET image = #{image,jdbcType=VARCHAR}
        WHERE id in
        <foreach collection="ids" item="id" index="index"
                 open="(" close=")" separator=",">
            #{id,jdbcType=BIGINT}
        </foreach>
    </update>
</mapper>

