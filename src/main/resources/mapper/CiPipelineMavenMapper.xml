<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="io.choerodon.devops.infra.mapper.CiPipelineMavenMapper">

    <select id="queryPipelineLatestMaven" resultType="io.choerodon.devops.infra.dto.CiPipelineMavenDTO">
        select *
        from  devops_ci_pipeline_maven dcpm
        where dcpm.app_service_id = #{appServiceId}
          and dcpm.gitlab_pipeline_id = #{gitlabPipelineId}
        order by id desc
        limit 1
    </select>
</mapper>